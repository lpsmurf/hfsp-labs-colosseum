# Audit API base URL
AUDIT_API=https://audit.hfsp.xyz

# x402 payer key for the paid tiers. Either is enough — the challenge offers
# both Base and Solana USDC and the client picks.
EVM_PRIVATE_KEY=
SOLANA_PRIVATE_KEY=

# Optional: skip the payment flow and reuse an already-settled transaction
# (legacy X-Payment path).
PAID_TX_HASH=
