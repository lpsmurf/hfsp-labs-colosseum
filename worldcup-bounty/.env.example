# TxLine (TxODDS) — World Cup free tier
TXLINE_BASE_URL=https://txline.txodds.com
# After on-chain subscribe (service level 1 or 12) + activate:
TXLINE_JWT=
TXLINE_API_TOKEN=
# Confirm the World Cup competitionId from /api/fixtures/snapshot output:
TXLINE_WC_COMPETITION_ID=500005
