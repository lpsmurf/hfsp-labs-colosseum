{
  "name": "gnosis-card-contracts",
  "version": "0.1.0",
  "description": "Gnosis Pay x402 — on-chain contracts",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network baseSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network baseMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "@semaphore-protocol/contracts": "^4.5.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.4.0"
  }
}
